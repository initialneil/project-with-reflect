---
name: {{NAME}}
description: "Use the {{NAME}} API (HTTP/WebSocket) — call its endpoints with the key from env, per its documented usage + learned quirks. Use when the user types /{{NAME}}, mentions the {{NAME}} API/service, or asks to call / query / transcribe / use it."
---
# {{NAME}} — API connection skill (project-with-reflect)

Facts live in this folder's `{{NAME}}.md` frontmatter (`base_url`, `key_env`) and `connection.json`.
**The key lives in `$<key_env>` (env/keychain) — never printed, logged, or written to disk.**

## Before acting
1. Load `{{NAME}}.md` — base URL, endpoints, usage, and learned quirks (`## Quirks`). Apply them.
2. **Ground in the docs.** When implementing or deciding against this API, **fetch `docs_url`**
   (frontmatter) for authoritative endpoints/params/limits — don't guess; cite what you used.
3. Read the key from the env var named by `key_env` at call time; if it's unset, tell the user to
   set it — don't proceed with a blank key.
4. Auto-log notable events to `log.md`: a working call recipe, an error + fix, a rate/cost gotcha.

## Actions
- `<action> [args]` — make the documented call (curl / SDK / WebSocket) to the right endpoint
  from `{{NAME}}.md`, authenticating with `$<key_env>`. Return the result; **never echo the key**.
- `checkin` — **pick up + get ready**: verify `$<key_env>` is set (optional cheap health ping to
  `base_url`), apply learned quirks, **silently set the terminal title** (`term-title.sh "{{NAME}}"` —
  no-ops off a real terminal; never surface its output), then auto-run `status`. **Never echo the key.**
- `status` — a **smart brief**: the facts (`base_url` / `key_env`) + **key set? / reachable?** + quirk
  count + the most recent `log.md` line. Read-only; never prints the key.
- `update "<content>"` — fold reference material into this note's body sections (`## Endpoints`,
  `## Setup`, `## Usage`, `## Gotchas`): merge + dedupe, lean — **don't blind-append**. **Secrets
  stay in env** (an API key or "let me edit" → an env pointer, never the key). Frontmatter facts
  aren't touched here — re-run register-api to change `base_url` / `key_env`.
- `note "…"` — force a line into `log.md`.
- `reflect` — fold `log.md` into the `## Quirks` section of `{{NAME}}.md`, then archive
  (`reflect.sh archive-entity {{DIR}}`).
- `help` — this menu.

Endpoints / rate limits / billing / gotchas live in `{{NAME}}.md`; reflect maintains them. Lean.
