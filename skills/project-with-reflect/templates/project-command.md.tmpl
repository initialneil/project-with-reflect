---
description: "{{NAME}} — work on this project with persistent, self-improving context (project-with-reflect)"
argument-hint: "[bootstrap | status | reflect | streams | register-branch | use-knowledge | <branch> | note | help]"
---
# /{{NAME}}

Work on the **{{NAME}}** project. Its persistent memory lives in `{{PDIR}}`.

**Read `{{PDIR}}/SKILL.md` and follow it** — it carries the behavioral contract
(load `{{NAME}}.md` + `decisions.md` + the active workstream + matching rule modules
*before* proposing anything) and every subcommand handler.

Arguments after `/{{NAME}}` select the action; with none, just do the work the user
asks and auto-log key events to the active workstream's `log.md`.

Common: `bootstrap` · `status` · `list` · `help` · `reflect [--reground]` · `streams` ·
`register-branch <b> --base <x>` · `<branch> [pr|rebase|reset]` ·
`register-eval <e> [path]` · `eval all` · `register-task <t>` · `use-knowledge <k>` · `note "…"`.

$ARGUMENTS
